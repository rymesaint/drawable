name: drawable
description: Flutter plugin to load Android drawables. This helps to share graphics between Flutter and Android
version: 0.2.0
homepage: https://github.com/rymesaint/drawable
repository: https://github.com/rymesaint/drawable
issue_tracker: https://github.com/rymesaint/drawable/issues

environment:
  sdk: ">=2.15.0-0 <3.0.0"
  flutter: '>=1.26.0-17.5.pre'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.10.0

flutter:
  plugin:
    platforms:
      android:
        package: de.ju.drawable
        pluginClass: DrawablePlugin